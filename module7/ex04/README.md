# 42 Barcelona | Discovery Piscine

## 🇬🇧 Module7 - Python

| Exercise 04: | *Just one step away!* |
|:-|:-|
| Turn-in directory: | **ex04/** |
| Files to turn in: | **free_range.py** |
| Allowed functions: | **All** |

- Create a program called free_range.py.
- Ensure this program is executable.
- This program should take 2 numbers as parameters.
- The first number must be strictly smaller than the second one.
- You must construct an array containing all the values between these two numbers.
- You must display the array using the `print` function.
- If the number of parameters is different from 2, the program displays `none` followed by a newline.

```shell
?> ./free_range.py | cat -e
none$
?> ./free_range.py 10 14 | cat -e
[10, 11, 12, 13, 14]$
?>
```

> 💡 Use the `range` function.

---

<details>
<summary>🇪🇸 Español</summary>

## Módulo7 - Python

| Ejercicio 04: | *¡A un paso más!* |
|:-|:-|
| Directorio de entrega: | **ex04/** |
| Archivos a entregar: | **free_range.py** |
| Funciones autorizadas: | **Todas** |

- Crea un programa llamado **free_range.py**.
- Asegúrate de que este programa sea ejecutable.
- Este programa debe recibir 2 números como parámetros.
- El primer número debe ser estrictamente menor que el segundo número.
- Debes construir un array que contenga todos los valores entre estos dos números.
- Debes mostrar el array usando la función `print`.
- Si el número de parámetros es diferente de 2, el programa debe mostrar `none` seguido de un salto de línea.

```shell
?> ./free_range.py | cat -e
none$
?> ./free_range.py 10 14 | cat -e
[10, 11, 12, 13, 14]$
?>
```

> 💡 Usa la función `range`.

</details>
