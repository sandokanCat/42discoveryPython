# 42 Barcelona | Discovery Piscine

## 🇬🇧 Module7 - Python

| Exercise 01: | *Counting and Measuring Parameters* |
|:-|:-|
| Turn-in directory: | **ex01/** |
| Files to turn in: | **count_it.py** |
| Allowed functions: | **All** |

- Create a program called **count_it.py**.
- Ensure this program is executable.
- The program should:
  - Display `parameters:` followed by the total number of parameters passed as arguments.
  - For each parameter, display the parameter itself and its length, ending with a newline.
  - If no parameters are provided, display `none` followed by a newline.

```shell
?> ./count_it.py | cat -e
none$
?> ./count_it.py "Game" "of" "Thrones" | cat -e
parameters: 3$
Game: 4$
of: 2$
Thrones: 7$
?>
```

> ⚠️ This time, you must use a `for` loop instead of a `while` loop.

---

<details>
<summary>🇪🇸 Español</summary>

## Módulo7 - Python

| Ejercicio 01: | *Contar y Medir Parámetros* |
|:-|:-|
| Directorio de entrega: | **ex01/** |
| Archivos a entregar: | **count_it.py** |
| Funciones autorizadas: | **Todas** |

- Crea un programa llamado **count_it.py**.
- Asegúrate de que este programa sea ejecutable.
- El programa debe:
  - Mostrar `parameters:` seguido del número total de parámetros pasados como argumentos.
  - Para cada parámetro, mostrar el parámetro en sí y su longitud, terminando con un salto de línea.
  - Si no se proporcionan parámetros, muestra `none` seguido de un salto de línea.

```shell
?> ./count_it.py | cat -e
none$
?> ./count_it.py "Game" "of" "Thrones" | cat -e
parameters: 3$
Game: 4$
of: 2$
Thrones: 7$
?>
```

> ⚠️ Esta vez, debes usar un bucle `for`lugar de un bucle `while`.

</details>
