# 42 Barcelona | Discovery Piscine

## 🇬🇧 Module7 - Python

| Exercise 00: | *Identifying a Parameter* |
|:-|:-|
| Turn-in directory: | **ex00/** |
| Files to turn in: | **parameter_matching.py** |
| Allowed functions: | **All** |

- Create a program called **parameter_matching.py**.
- Ensure this program is executable.
- The program should:
  - Take a string as a parameter.
  - Prompt the user to enter a word that matches the parameter passed as an argument, as shown in the example below.
  - Display `Good job!` followed by a newline if the word entered by the user matches the parameter passed.
  - Display `Nope, sorry...` followed by a newline if the word entered by the user does not match the parameter passed.
  - Display `none` followed by a newline if the number of parameters passed is different from 1.

```shell
?> ./parameter_matching.py
none
?> ./parameter_matching.py "Hello"
What was the parameter? Bonjour
Nope, sorry...
?> ./parameter_matching.py "Hello"
What was the parameter? Hello
Good job!
?>
```

---

<details>
<summary>🇪🇸 Español</summary>

## Módulo7 - Python

| Ejercicio 00: | *Identificar un Parámetro* |
|:-|:-|
| Directorio de entrega: | **ex00/** |
| Archivos a entregar: | **parameter_matching.py** |
| Funciones autorizadas: | **Todas** |

- Crea un programa llamado **parameter_matching.py**.
- Asegúrate de que este programa sea ejecutable.
- El programa debe:
  - Tomar un string como parámetro.
  - Pedir al usuario que ingrese una palabra que coincida con el parámetro pasado como argumento, como se muestra en el ejemplo a continuación.
  - Mostrar `Good job!` seguido de un salto de línea si la palabra ingresada por el usuario coincide con el parámetro pasado.
  - Mostrar `Nope, sorry...` seguido de un salto de línea si la palabra ingresada por el usuario no coincide con el parámetro pasado.
  - Mostrar `none` seguido de un salto de línea si el número de parámetros pasados es diferente de 1.

```shell
?> ./parameter_matching.py
none
?> ./parameter_matching.py "Hello"
What was the parameter? Bonjour
Nope, sorry...
?> ./parameter_matching.py "Hello"
What was the parameter? Hello
Good job!
?>
```

</details>
