# 42 Barcelona | Discovery Piscine

## 🇬🇧 Module7 - Python

| Exercise 03: | *Modifying Strings* |
|:-|:-|
| Turn-in directory: | **ex03/** |
| Files to turn in: | **append_it.py** |
| Allowed functions: | **All** |

- Create a program called **append_it.py**.
- Ensure this program is executable.
- The program should display each parameter passed as an argument, one by one, by appending it with `ism`.
- If a parameter already ends with `ism`, it should be skipped and not displayed.
- If there are no parameters, the program should display `none` followed by a newline.

```shell
?> ./append_it.py | cat -e
none$
?> ./append_it.py "parallel" "egoism" "human" | cat -e
parallelism$
humanism$
?>
```

> 💡 Use find.

---

<details>
<summary>🇪🇸 Español</summary>

## Módulo7 - Python

| Ejercicio 03: | *Modificando Strings* |
|:-|:-|
| Directorio de entrega: | **ex03/** |
| Archivos a entregar: | **append_it.py** |
| Funciones autorizadas: | **Todas** |

- Crea un programa llamado **append_it.py**.
- Asegúrate de que este programa sea ejecutable.
- El programa debe mostrar cada parámetro pasado como argumento, uno por uno, añadiéndole `ism`.
- Si un parámetro ya termina con `ism`, debe ser omitido y no mostrado.
- Si no se proporcionan parámetros, el programa debe mostrar `none` seguido de un salto de línea.

```shell
?> ./append_it.py | cat -e
none$
?> ./append_it.py "parallel" "egoism" "human" | cat -e
parallelism$
humanism$
?>
```

> 💡 Usa find.

</details>
