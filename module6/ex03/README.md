# 42 Barcelona | Discovery Piscine

## 🇬🇧 Module6 - Python

| Exercise 03: | *Display an Array in Reverse Order* |
|:-|:-|
| Turn-in directory: | **ex03/** |
| Files to turn in: | **aff_rev_params.py** |
| Allowed functions: | **All** |

- Create a program called **aff_rev_params.py**.
- Ensure this program is executable.
- When executed, the program should display all the strings passed as parameters, followed by a newline, in reverse order.
- If there are fewer than two parameters, it should display `none` followed by a newline.

```shell
?> ./aff_rev_params.py | cat -e
none$
?> ./aff_rev_params.py "coucou" | cat -e
none$
?> ./aff_rev_params.py "Python" "piscine" "hello" | cat -e
hello$
piscine$
Python$
?>
```

---

<details>
<summary>🇪🇸 Español</summary>

## Módulo6 - Python

| Ejercicio 03: | *Mostrar un Array en Orden Inverso* |
|:-|:-|
| Directorio de entrega: | **ex03/** |
| Archivos a entregar: | **aff_rev_params.py** |
| Funciones autorizadas: | **Todas** |

- Crea un programa llamado **aff_rev_params.py**.
- Asegúrate de que este programa sea ejecutable.
- Cuando se ejecute, el programa debe mostrar todos los strings pasados como parámetros, seguidos de un salto de línea, en orden invertido.
- Si hay menos de dos parámetros, debe mostrar "none" seguido de un salto de línea.

```shell
?> ./aff_rev_params.py | cat -e
none$
?> ./aff_rev_params.py "coucou" | cat -e
none$
?> ./aff_rev_params.py "Python" "piscine" "hello" | cat -e
hello$
piscine$
Python$
?>
```

</details>
